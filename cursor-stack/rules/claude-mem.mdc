---
description: Persistent memory — что сохранять между сессиями через Qdrant. Применяй когда фиксируешь архитектурные решения, баги, конвенции.
globs:
alwaysApply: false
---

# Memory — что сохранять в Qdrant

Сохранение контекста между сессиями: архитектурные решения, технические наблюдения, решённые проблемы.

## Что фиксировать

- **Архитектура** — почему выбран подход, отклонённые альтернативы, ключевые паттерны
- **Техника** — настройка окружения, зависимости, конфиги, обходные пути
- **Проект** — структура кодовой базы, паттерны стиля, workflow, тестовые окружения
- **Решённые проблемы** — сложные баги, корневые причины, подводные камни

## Формат записи

Кратко (1-3 предложения), конкретно (точные файлы/команды), полезно для будущих сессий.

Пример:
```
БД: PostgreSQL 15, порт 5432, .env.local
Миграции: `npm run db:migrate` | Reset: `npm run db:reset`
```

```
API: REST/Express, src/routes/, JWT auth (src/middleware/auth.ts)
```

## Когда писать в память

1. Начало сессии → прочитай контекст из Qdrant
2. Решил сложную проблему → запиши причину и фикс
3. Нашёл паттерн → запиши где применяется
4. Конец сессии → сохрани ключевые наблюдения

## MCP инструменты

В этом стеке доступен `qdrant-memory` MCP сервер. Используй его инструменты для записи/поиска памяти.
